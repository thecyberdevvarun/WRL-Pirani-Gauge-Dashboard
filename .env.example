# SQL Server connection used by db.py
DB_SERVER=10.100.95.160
DB_NAME=Garuda_WRL_LIVE
DB_USER=sa
DB_PASSWORD=TnTMES@2022

# Modbus TCP gateway used by config.py
MODBUS_HOST=192.168.1.3
MODBUS_PORT=502

# Set to 1 to disable the background Modbus/fixture polling threads
# (useful for running tests or a quick syntax check without hardware).
DISABLE_WORKERS=0
